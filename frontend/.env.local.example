# Frontend Environment Variables

# API URL - Points to the backend API
NEXT_PUBLIC_API_URL=http://localhost:8000

# Note: When running with Docker Compose, the API_URL is set automatically
# For local development without Docker, make sure the backend is running on port 8000
